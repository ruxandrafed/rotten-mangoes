<%= form_tag("/movies", method: "get") do %>
  <p>
    <%= label_tag(:term, "Search by title or director:") %>
    <%= text_field_tag(:term) %>
  </p>
  <%= submit_tag("Search") %>
  <%= button_tag "Reset", type: :reset %>
<% end %>
